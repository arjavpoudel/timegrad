# Multivariate Probabilistic Forecasting for Neural Dynamics

This model combines RNNs + a diffusion component to train on synthetic 2d ornstein uhlenbeck data, and generate realistic trajectories. the plan is to scale up to rela neural datasets.
